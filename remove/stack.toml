[stack]
name = "demo"
version = "0.1.0"
description = "postgres + redis, registry kegs only"

[[app]]
run = "postgres@17"
name = "db"
publish = ["internal:15432:5432"]
params = { database = "demo", password = "dev" } # password stays minted
